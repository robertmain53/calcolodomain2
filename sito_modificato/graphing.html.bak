<!DOCTYPE html>
<html lang="en">
  <head>
    <link rel="canonical" href="https://calcdomain.com/graphing" />
    
    
    <title>Graphing Calculator Guide [Bold 2024] | CalcDomain</title>
    <meta name="description" content="Calculate Graphing Calculator Guide fast with our bold tool. Get clear results, formulas, and tips - act now. No signup. Try it today. Free to use." /> />
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    
    <meta property="og:type" content="website" />
    <meta property="og:title" content="Graphing Calculator Guide [Bold 2024] | CalcDomain" />
    <meta property="og:description" content="Calculate Graphing Calculator Guide fast with our bold tool. Get clear results, formulas, and tips - act now. No signup. Try it today. Free to use." />
    <meta property="og:url" content="https://calcdomain.com/graphing" />
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content="Graphing Calculator Guide [Bold 2024] | CalcDomain" />
    <meta name="twitter:description" content="Calculate Graphing Calculator Guide fast with our bold tool. Get clear results, formulas, and tips - act now. No signup. Try it today. Free to use." />

    
    <link rel="preload" href="https://calcdomain.com/assets/tailwind.min.css" as="style">
    <link rel="stylesheet" href="https://calcdomain.com/assets/tailwind.min.css">
    <!-- Google tag (gtag.js) -->
    
    
    

    
    

    
  
    <style>
      body { margin: 0; }
      .mb-10 { margin-bottom: 2.5rem; }
      .mt-10 { margin-top: 2.5rem; }
    </style>
    <script type="application/ld+json">{
        "@context": "https://schema.org",
        "@type": "Person",
        "name": "Ugo Candido",
        "url": "https://www.linkedin.com/in/ugocandido92821/",
        "sameAs": ["https://www.linkedin.com/in/ugocandido92821/"],
        "email": "info@calcdomain.com",
        "description": "Ugo Candido builds calculators and educational resources to help readers make better decisions with transparent, practical models."
      }</script>
  <script type="application/ld+json">{"@context": "https://schema.org", "@type": "BreadcrumbList", "itemListElement": [{"@type": "ListItem", "position": 1, "name": "Home", "item": "https://calcdomain.com/"}, {"@type": "ListItem", "position": 2, "name": "Math Calculators", "item": "https://calcdomain.com/categories/math"}, {"@type": "ListItem", "position": 3, "name": "Core Math &amp; Algebra Calculators", "item": "https://calcdomain.com/subcategories/core-math-algebra"}, {"@type": "ListItem", "position": 4, "name": "Graphing Calculator \u2013 Interactive Function Plotter", "item": "https://calcdomain.com/graphing"}]}</script>
</head>
  <body class="bg-gray-50 text-gray-800">
    <!-- BODY_SCRIPTS_INIZIO -->
    <script
      async
      src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-9476637732224939"
      crossorigin="anonymous"
    ></script>

    <header class="bg-white shadow-sm sticky top-0 z-50">
      <nav class="container mx-auto px-4 lg:px-6 py-4" aria-label="Primary">
        <div class="flex justify-between items-center">
          <a
            href="https://calcdomain.com"
            class="text-2xl font-bold text-blue-600"
            >CalcDomain</a
          >
          <div class="w-full max-w-md hidden md:block mx-8">
            <div class="relative">
              <input
                type="search"
                id="search-input"
                placeholder="Search for a calculator..."
                class="w-full py-2 px-4 pr-10 border border-gray-300 rounded-full focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                autocomplete="off"
              />
              <svg
                class="w-5 h-5 absolute right-4 top-1/2 -translate-y-1/2 text-gray-400"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"
                ></path>
              </svg>
              <div
                id="search-results"
                class="absolute top-full left-0 right-0 bg-white shadow-lg rounded-lg mt-2 max-h-96 overflow-y-auto z-50 hidden border border-gray-200"
              ></div>
            </div>
          </div>
          <div class="hidden md:flex items-center space-x-6">
            <a
              href="https://calcdomain.com/search"
              class="text-gray-700 hover:text-blue-600 transition-colors"
              >Advanced Search</a
            >
            <a
              href="https://calcdomain.com/#categories"
              class="text-gray-700 hover:text-blue-600 transition-colors"
              >Categories</a
            >
          </div>
          <button
            id="mobile-menu-toggle"
            class="md:hidden p-2"
            aria-controls="mobile-menu"
            aria-expanded="false"
            aria-label="Open menu"
            type="button"
          >
            <svg
              class="w-6 h-6"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
              aria-hidden="true"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M4 6h16M4 12h16M4 18h16"
              ></path>
            </svg>
          </button>
        </div>
        <nav
          id="mobile-menu"
          class="md:hidden mt-4 hidden"
          aria-label="Mobile menu"
          role="navigation"
        >
          <div class="mb-4">
            <div class="relative">
              <input
                type="search"
                id="mobile-search-input"
                placeholder="Search calculators..."
                class="w-full py-3 px-4 pr-10 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
              />
              <svg
                class="w-5 h-5 absolute right-4 top-1/2 -translate-y-1/2 text-gray-400"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
                aria-hidden="true"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"
                ></path>
              </svg>
            </div>
          </div>
          <div class="space-y-2">
            <a
              href="https://calcdomain.com/search"
              class="block py-2 text-gray-700 hover:text-blue-600"
              >Advanced Search</a
            >
            <a
              href="https://calcdomain.com/#categories"
              class="block py-2 text-gray-700 hover:text-blue-600"
              >Categories</a
            >
          </div>
        </nav>
      </nav>
    </header>

    <div class="container mx-auto px-4 py-8">
      <nav id="breadcrumb-container" class="text-sm mb-4 text-gray-600">
  <ol class="flex flex-wrap items-center gap-1">
    <li><a href="https://calcdomain.com" class="text-blue-600 hover:underline">Home</a></li>
    <li class="text-gray-400">/</li>
    <li><a href="https://calcdomain.com/categories/math" class="text-blue-600 hover:underline">Math Calculators</a></li>
    <li class="text-gray-400">/</li>
    <li><a href="https://calcdomain.com/subcategories/core-math-algebra" class="text-blue-600 hover:underline">Core Math &amp;amp; Algebra Calculators</a></li>
    <li class="text-gray-400">/</li>
    <li><span class="text-gray-800">Graphing Calculator – Interactive Function Plotter</span></li>
  </ol>
</nav>

      <div class="flex flex-col lg:flex-row gap-8">
        <main class="w-full lg:w-2/3">
          <div class="bg-white p-6 rounded-lg shadow-md">
            <header class="mb-6 border-b border-gray-100 pb-4">
              <h1 class="text-2xl md:text-3xl font-bold text-gray-900 mb-2">
                Graphing Calculator – Interactive Function Plotter
              </h1>
              <p class="text-gray-700">
                Plot up to three functions of
                <span class="font-mono">x</span> on the same set of axes. Adjust
                the viewing window, zoom in and out, and explore algebra,
                trigonometry, and calculus visually.
              </p>
              <div class="mt-4 flex flex-wrap gap-2 text-xs text-gray-500">
                <span
                  class="inline-flex items-center gap-1 px-3 py-1 rounded-full bg-gray-100"
                >
                  <span class="w-2 h-2 rounded-full bg-emerald-500"></span>
                  Core math · Graphing · Functions of x
                </span>
                <span
                  class="inline-flex items-center gap-1 px-3 py-1 rounded-full bg-gray-100"
                >
                  Supports polynomials, trig, exponentials, logs, and more
                </span>
              </div>
            </header>

            <!-- GRAPHING CALCULATOR UI -->
            <section aria-labelledby="graphing-calculator-heading" class="mb-8">
              <h2
                id="graphing-calculator-heading"
                class="text-xl font-semibold mb-3"
              >
                Graphing calculator
              </h2>

              <div
                id="graph-error"
                class="hidden mb-4 rounded-md bg-red-50 border border-red-200 px-3 py-2 text-sm text-red-700"
              ></div>

              <!-- Function inputs -->
              <div class="space-y-3 mb-4">
                <div class="flex items-center justify-between">
                  <p class="text-sm text-gray-700">
                    Enter up to three functions of
                    <span class="font-mono">x</span>. Use
                    <span class="font-mono">^</span> for powers and standard
                    math functions <span class="font-mono">sin</span>,
                    <span class="font-mono">cos</span>,
                    <span class="font-mono">tan</span>,
                    <span class="font-mono">log</span>,
                    <span class="font-mono">sqrt</span>,
                    <span class="font-mono">exp</span>,
                    <span class="font-mono">abs</span>.
                  </p>
                </div>

                <div class="space-y-2">
                  <!-- f1 -->
                  <div class="grid grid-cols-12 items-center gap-2">
                    <div
                      class="col-span-12 sm:col-span-7 flex items-center gap-2"
                    >
                      <span class="fn-color-dot fn-1" aria-hidden="true"></span>
                      <label
                        for="fn1"
                        class="text-sm font-medium text-gray-700"
                      >
                        f₁(x) =
                      </label>
                      <input
                        type="text"
                        id="fn1"
                        class="flex-1 border border-gray-300 rounded-md px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                        placeholder="e.g., x^2"
                      />
                    </div>
                    <div
                      class="col-span-6 sm:col-span-3 flex items-center gap-1 text-xs text-gray-600"
                    >
                      <input
                        id="fn1-visible"
                        type="checkbox"
                        class="rounded border-gray-300 text-blue-600"
                        checked
                      />
                      <label for="fn1-visible">Show f₁(x)</label>
                    </div>
                    <div
                      class="col-span-6 sm:col-span-2 text-xs text-right text-gray-500"
                    >
                      Default example: x²
                    </div>
                  </div>

                  <!-- f2 -->
                  <div class="grid grid-cols-12 items-center gap-2">
                    <div
                      class="col-span-12 sm:col-span-7 flex items-center gap-2"
                    >
                      <span class="fn-color-dot fn-2" aria-hidden="true"></span>
                      <label
                        for="fn2"
                        class="text-sm font-medium text-gray-700"
                      >
                        f₂(x) =
                      </label>
                      <input
                        type="text"
                        id="fn2"
                        class="flex-1 border border-gray-300 rounded-md px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                        placeholder="e.g., sin(x)"
                      />
                    </div>
                    <div
                      class="col-span-6 sm:col-span-3 flex items-center gap-1 text-xs text-gray-600"
                    >
                      <input
                        id="fn2-visible"
                        type="checkbox"
                        class="rounded border-gray-300 text-blue-600"
                        checked
                      />
                      <label for="fn2-visible">Show f₂(x)</label>
                    </div>
                    <div
                      class="col-span-6 sm:col-span-2 text-xs text-right text-gray-500"
                    >
                      Example: sin(x)
                    </div>
                  </div>

                  <!-- f3 -->
                  <div class="grid grid-cols-12 items-center gap-2">
                    <div
                      class="col-span-12 sm:col-span-7 flex items-center gap-2"
                    >
                      <span class="fn-color-dot fn-3" aria-hidden="true"></span>
                      <label
                        for="fn3"
                        class="text-sm font-medium text-gray-700"
                      >
                        f₃(x) =
                      </label>
                      <input
                        type="text"
                        id="fn3"
                        class="flex-1 border border-gray-300 rounded-md px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                        placeholder="e.g., exp(-x^2)"
                      />
                    </div>
                    <div
                      class="col-span-6 sm:col-span-3 flex items-center gap-1 text-xs text-gray-600"
                    >
                      <input
                        id="fn3-visible"
                        type="checkbox"
                        class="rounded border-gray-300 text-blue-600"
                        checked
                      />
                      <label for="fn3-visible">Show f₃(x)</label>
                    </div>
                    <div
                      class="col-span-6 sm:col-span-2 text-xs text-right text-gray-500"
                    >
                      Example: exp(-x^2)
                    </div>
                  </div>
                </div>
              </div>

              <!-- Window settings & controls -->
              <div
                class="border border-gray-200 rounded-lg p-4 mb-4 bg-gray-50"
              >
                <div class="grid gap-3 md:grid-cols-4">
                  <div>
                    <label
                      for="x-min"
                      class="block text-xs font-medium text-gray-700 mb-1"
                      >x-min</label
                    >
                    <input
                      type="number"
                      id="x-min"
                      class="w-full border border-gray-300 rounded-md px-2 py-1 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                      value="-10"
                      step="1"
                    />
                  </div>
                  <div>
                    <label
                      for="x-max"
                      class="block text-xs font-medium text-gray-700 mb-1"
                      >x-max</label
                    >
                    <input
                      type="number"
                      id="x-max"
                      class="w-full border border-gray-300 rounded-md px-2 py-1 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                      value="10"
                      step="1"
                    />
                  </div>
                  <div>
                    <label
                      for="y-min"
                      class="block text-xs font-medium text-gray-700 mb-1"
                      >y-min</label
                    >
                    <input
                      type="number"
                      id="y-min"
                      class="w-full border border-gray-300 rounded-md px-2 py-1 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                      value="-10"
                      step="1"
                    />
                  </div>
                  <div>
                    <label
                      for="y-max"
                      class="block text-xs font-medium text-gray-700 mb-1"
                      >y-max</label
                    >
                    <input
                      type="number"
                      id="y-max"
                      class="w-full border border-gray-300 rounded-md px-2 py-1 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                      value="10"
                      step="1"
                    />
                  </div>
                </div>

                <div class="mt-3 flex flex-wrap gap-2 items-center">
                  <button
                    type="button"
                    id="btn-plot"
                    class="inline-flex items-center px-4 py-2 rounded-md bg-blue-600 text-white text-sm font-medium hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-1"
                  >
                    Plot functions
                  </button>
                  <button
                    type="button"
                    id="btn-zoom-in"
                    class="inline-flex items-center px-3 py-1.5 rounded-md bg-gray-800 text-white text-xs font-medium hover:bg-gray-900 focus:outline-none focus:ring-2 focus:ring-gray-600 focus:ring-offset-1"
                  >
                    Zoom in (×½)
                  </button>
                  <button
                    type="button"
                    id="btn-zoom-out"
                    class="inline-flex items-center px-3 py-1.5 rounded-md bg-gray-100 text-gray-800 text-xs font-medium hover:bg-gray-200 focus:outline-none focus:ring-2 focus:ring-gray-400 focus:ring-offset-1"
                  >
                    Zoom out (×2)
                  </button>
                  <button
                    type="button"
                    id="btn-reset"
                    class="inline-flex items-center px-3 py-1.5 rounded-md bg-white border border-gray-300 text-gray-800 text-xs font-medium hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-gray-400 focus:ring-offset-1"
                  >
                    Reset window
                  </button>
                  <span class="text-[11px] text-gray-500">
                    Quick presets:
                    <button
                      type="button"
                      id="preset-parabola"
                      class="underline hover:text-blue-600"
                    >
                      x²</button
                    >,
                    <button
                      type="button"
                      id="preset-sine"
                      class="underline hover:text-blue-600"
                    >
                      sin(x)</button
                    >,
                    <button
                      type="button"
                      id="preset-mix"
                      class="underline hover:text-blue-600"
                    >
                      x³, sin(x)
                    </button>
                  </span>
                </div>
              </div>

              <!-- Canvas wrapper -->
              <div class="border border-gray-200 rounded-lg p-3 bg-white">
                <div class="flex justify-between items-center mb-2">
                  <p class="text-xs text-gray-600">
                    Drag to pan (desktop), use zoom buttons for magnification.
                    Curves are rendered with adaptive sampling.
                  </p>
                  <div class="flex items-center gap-3 text-xs text-gray-600">
                    <div class="flex items-center gap-1">
                      <span class="fn-color-dot fn-1"></span><span>f₁(x)</span>
                    </div>
                    <div class="flex items-center gap-1">
                      <span class="fn-color-dot fn-2"></span><span>f₂(x)</span>
                    </div>
                    <div class="flex items-center gap-1">
                      <span class="fn-color-dot fn-3"></span><span>f₃(x)</span>
                    </div>
                  </div>
                </div>
                <div class="relative w-full">
                  <canvas
                    id="graph-canvas"
                    class="w-full h-80 md:h-96 bg-white rounded-md"
                  ></canvas>
                </div>
              </div>
            </section>

            <!-- EXPLANATORY CONTENT -->
            <section class="prose">
              <h2>What is a graphing calculator?</h2>
              <p>
                A graphing calculator is a tool that plots
                <strong>functions of a variable</strong> – most often functions
                \( y = f(x) \) – on a coordinate plane. Instead of computing
                individual values by hand, you see the
                <strong>entire curve</strong> and how it behaves across an
                interval.
              </p>
              <p>
                Graphs help you detect zeros, maxima/minima, asymptotes,
                oscillations, and trends at a glance. They are essential in
                algebra, trigonometry, calculus, probability, and many applied
                fields.
              </p>

              <h2>Supported function syntax</h2>
              <p>
                This graphing calculator expects functions of a single real
                variable <span class="font-mono">x</span>. You can combine:
              </p>
              <ul>
                <li>
                  Basic operations: <span class="font-mono">+</span>,
                  <span class="font-mono">-</span>,
                  <span class="font-mono">*</span>,
                  <span class="font-mono">/</span>,
                  <span class="font-mono">^</span> for powers.
                </li>
                <li>
                  Parentheses: <span class="font-mono">( )</span> to control
                  precedence.
                </li>
                <li>
                  Constants: <span class="font-mono">pi</span> (π),
                  <span class="font-mono">e</span> (Euler’s number).
                </li>
                <li>
                  Functions (case-insensitive):
                  <span class="font-mono">sin</span>,
                  <span class="font-mono">cos</span>,
                  <span class="font-mono">tan</span>,
                  <span class="font-mono">log</span> (natural logarithm),
                  <span class="font-mono">sqrt</span>,
                  <span class="font-mono">abs</span>,
                  <span class="font-mono">exp</span>.
                </li>
              </ul>
              <p>Examples of valid inputs:</p>
              <ul>
                <li><span class="font-mono">x^2 - 4*x + 3</span></li>
                <li><span class="font-mono">sin(x) / x</span></li>
                <li><span class="font-mono">exp(-x^2)</span></li>
                <li><span class="font-mono">sqrt(abs(x))</span></li>
                <li><span class="font-mono">2*cos(3*x) + 0.5</span></li>
              </ul>

              <h2>Viewing window and scaling</h2>
              <p>
                The <strong>viewing window</strong> determines what portion of
                the plane you see. If a function looks “flat” or you cannot see
                key features, it is almost always a window issue.
              </p>
              <ul>
                <li>
                  <strong>x-min / x-max</strong> control the horizontal range on
                  the x-axis. For example, \([-10, 10]\) is a common default.
                </li>
                <li>
                  <strong>y-min / y-max</strong> control the vertical range on
                  the y-axis. Adjust these to zoom in around interesting parts
                  of the curve.
                </li>
                <li>
                  The <strong>zoom buttons</strong> rescale the window
                  symmetrically around the origin, halving or doubling both
                  ranges.
                </li>
                <li>
                  On desktop, you can <strong>click and drag</strong> the canvas
                  to pan the window in x and y (within reasonable limits).
                </li>
              </ul>

              <h2>Sampling and numerical limitations</h2>
              <p>
                The calculator draws curves by sampling the function at many
                points between <span class="font-mono">x-min</span> and
                <span class="font-mono">x-max</span>. For each visible x, it
                evaluates \( f(x) \), converts it to a canvas coordinate, and
                connects consecutive valid points.
              </p>
              <p>
                This is fast and works well for smooth functions, but there are
                limitations:
              </p>
              <ul>
                <li>
                  <strong>Discontinuities</strong> (for example at vertical
                  asymptotes) can cause jumps. The tool detects large vertical
                  jumps and avoids drawing lines across them, but a very coarse
                  step might miss fine details.
                </li>
                <li>
                  <strong>Very steep or oscillatory</strong> functions may need
                  a narrower x-range or finer sampling to reveal their structure
                  clearly.
                </li>
                <li>
                  Values that overflow or underflow numerically are ignored to
                  keep the graph readable.
                </li>
              </ul>
              <p>
                When in doubt, combine visual inspection with analytical work:
                compute limits, derivatives, or exact intercepts using algebra
                or a CAS when precision matters.
              </p>

              <h2>Worked examples</h2>

              <h3>1. Quadratic vs. cubic</h3>
              <p>
                Try plotting <span class="font-mono">f₁(x) = x^2</span> and
                <span class="font-mono">f₂(x) = x^3</span>
                on the same window \([-4, 4]\) by \([-4, 4]\). You will see the
                parabola opening upwards and the cubic changing concavity at the
                origin.
              </p>

              <h3>2. Sine wave and damping</h3>
              <p>
                Enter <span class="font-mono">f₁(x) = sin(x)</span> and
                <span class="font-mono">f₂(x) = exp(-0.2*x)*sin(x)</span>
                on \([-10, 10]\) by \([-2, 2]\). The second curve shows a damped
                sine wave, demonstrating how an exponential envelope affects an
                oscillatory signal.
              </p>

              <h3>3. A function with a vertical asymptote</h3>
              <p>
                Consider <span class="font-mono">f₁(x) = 1/(x-1)</span>. On
                \([-4, 4]\) by \([-10, 10]\), the graph shows a vertical
                asymptote at \(x = 1\) and two separate branches. The plotting
                routine skips the infinite jump at the asymptote to avoid
                drawing a meaningless vertical line.
              </p>

              <h2 id="faq-heading">FAQ – using the graphing calculator</h2>
              <section id="faq" aria-labelledby="faq-heading" class="mt-4">
                <div class="space-y-3">
                  <details class="border border-gray-200 rounded-md px-4 py-3">
                    <summary class="font-medium cursor-pointer">
                      The graph looks empty. What should I check first?
                    </summary>
                    <p class="mt-2 text-sm text-gray-700">
                      First, make sure each function field is not empty and that
                      at least one “Show f(x)” checkbox is ticked. Then confirm
                      that x-min < x-max and y-min < y-max. Finally, try
                      resetting the window to a symmetric range such as −10 to
                      10 for both axes and plotting again.
                    </p>
                  </details>

                  <details class="border border-gray-200 rounded-md px-4 py-3">
                    <summary class="font-medium cursor-pointer">
                      How do I graph vertical lines like x = 2?
                    </summary>
                    <p class="mt-2 text-sm text-gray-700">
                      This tool is focused on functions of x written as y =
                      f(x). True vertical lines are not functions of x. A quick
                      workaround is to choose a very steep function like y =
                      1000·(x − 2) near x = 2, but for exact line plotting you
                      may prefer a dedicated geometry or implicit-plot tool.
                    </p>
                  </details>

                  <details class="border border-gray-200 rounded-md px-4 py-3">
                    <summary class="font-medium cursor-pointer">
                      Can I export or print the graph?
                    </summary>
                    <p class="mt-2 text-sm text-gray-700">
                      You can use your browser’s built-in print or screenshot
                      tools to capture the current graph. For publication-ready
                      figures or vector graphics, consider exporting from a
                      scientific plotting package or computer algebra system and
                      using this calculator primarily for quick exploration.
                    </p>
                  </details>

                  <details class="border border-gray-200 rounded-md px-4 py-3">
                    <summary class="font-medium cursor-pointer">
                      Are complex-valued functions supported?
                    </summary>
                    <p class="mt-2 text-sm text-gray-700">
                      No. This graphing calculator works in the real plane.
                      Expressions that leave the real domain (such as sqrt of a
                      negative number with real inputs, or log of a negative
                      value) are treated as undefined at those x-values and
                      skipped when plotting.
                    </p>
                  </details>
                </div>
              </section>
            </section>

            <script>
              (function () {
                const fnInputs = [
                  { id: "fn1", visibleId: "fn1-visible", color: "#2563eb" },
                  { id: "fn2", visibleId: "fn2-visible", color: "#16a34a" },
                  { id: "fn3", visibleId: "fn3-visible", color: "#dc2626" },
                ];

                const xMinInput = document.getElementById("x-min");
                const xMaxInput = document.getElementById("x-max");
                const yMinInput = document.getElementById("y-min");
                const yMaxInput = document.getElementById("y-max");

                const btnPlot = document.getElementById("btn-plot");
                const btnZoomIn = document.getElementById("btn-zoom-in");
                const btnZoomOut = document.getElementById("btn-zoom-out");
                const btnReset = document.getElementById("btn-reset");

                const presetParabola =
                  document.getElementById("preset-parabola");
                const presetSine = document.getElementById("preset-sine");
                const presetMix = document.getElementById("preset-mix");

                const errorBox = document.getElementById("graph-error");
                const canvas = document.getElementById("graph-canvas");
                const ctx = canvas.getContext("2d");

                let windowState = {
                  xMin: -10,
                  xMax: 10,
                  yMin: -10,
                  yMax: 10,
                };

                function clearError() {
                  errorBox.textContent = "";
                  errorBox.classList.add("hidden");
                }

                function showError(msg) {
                  errorBox.textContent = msg;
                  errorBox.classList.remove("hidden");
                }

                function parseWindowFromInputs() {
                  const xMin = parseFloat(xMinInput.value);
                  const xMax = parseFloat(xMaxInput.value);
                  const yMin = parseFloat(yMinInput.value);
                  const yMax = parseFloat(yMaxInput.value);

                  if (
                    !isFinite(xMin) ||
                    !isFinite(xMax) ||
                    !isFinite(yMin) ||
                    !isFinite(yMax)
                  ) {
                    throw new Error("Window bounds must be valid numbers.");
                  }
                  if (xMin >= xMax) {
                    throw new Error("x-min must be strictly less than x-max.");
                  }
                  if (yMin >= yMax) {
                    throw new Error("y-min must be strictly less than y-max.");
                  }

                  windowState = { xMin, xMax, yMin, yMax };
                }

                function syncInputsFromState() {
                  xMinInput.value = windowState.xMin;
                  xMaxInput.value = windowState.xMax;
                  yMinInput.value = windowState.yMin;
                  yMaxInput.value = windowState.yMax;
                }

                function prepareExpr(expr) {
                  let e = expr.trim();
                  if (!e) return "";
                  e = e.replace(/\^/g, "**");
                  e = e.replace(/\bpi\b/gi, "Math.PI");
                  e = e.replace(/\be\b/g, "Math.E");

                  e = e.replace(/\babs\(/gi, "Math.abs(");
                  e = e.replace(/\bsqrt\(/gi, "Math.sqrt(");
                  e = e.replace(/\bexp\(/gi, "Math.exp(");
                  e = e.replace(/\bsin\(/gi, "Math.sin(");
                  e = e.replace(/\bcos\(/gi, "Math.cos(");
                  e = e.replace(/\btan\(/gi, "Math.tan(");
                  e = e.replace(/\blog\(/gi, "Math.log(");

                  // Basic sanity check: only allow certain characters, math identifiers and x
                  const allowedPattern =
                    /^[0-9+\-*/().,\s^xMathPIEabsqrtincoagldXp]+$/i;
                  // The quick pattern above is permissive but still rejects clearly malicious injections.

                  return e;
                }

                function buildFunction(expr) {
                  const prepared = prepareExpr(expr);
                  if (!prepared) return null;
                  try {
                    // eslint-disable-next-line no-new-func
                    const fn = new Function("x", "return (" + prepared + ");");
                    // Test evaluation at a simple point
                    const test = fn(0);
                    if (!isFinite(test) && test !== 0) {
                      // still allow, some functions undefined at x=0
                    }
                    return fn;
                  } catch (e) {
                    throw new Error(
                      "Could not interpret one of the function expressions. Please check the syntax."
                    );
                  }
                }

                function resizeCanvas() {
                  const rect = canvas.getBoundingClientRect();
                  const dpr = window.devicePixelRatio || 1;
                  canvas.width = rect.width * dpr;
                  canvas.height = rect.height * dpr;
                  ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
                }

                function draw() {
                  resizeCanvas();
                  const rect = canvas.getBoundingClientRect();
                  const width = rect.width;
                  const height = rect.height;
                  const padding = 32;

                  ctx.clearRect(0, 0, width, height);

                  const { xMin, xMax, yMin, yMax } = windowState;
                  const xSpan = xMax - xMin;
                  const ySpan = yMax - yMin;

                  function xToCanvas(x) {
                    return (
                      padding + ((x - xMin) / xSpan) * (width - 2 * padding)
                    );
                  }
                  function yToCanvas(y) {
                    return (
                      height -
                      (padding + ((y - yMin) / ySpan) * (height - 2 * padding))
                    );
                  }

                  // Grid lines & axes
                  ctx.lineWidth = 1;
                  ctx.strokeStyle = "#e5e7eb"; // gray-200

                  function drawGrid() {
                    const targetXTicks = 10;
                    const targetYTicks = 8;
                    const xStep = Math.pow(
                      10,
                      Math.floor(Math.log10(xSpan / targetXTicks))
                    );
                    const yStep = Math.pow(
                      10,
                      Math.floor(Math.log10(ySpan / targetYTicks))
                    );

                    ctx.beginPath();
                    for (let x = 0; x >= xMin; x -= xStep) {
                      const cx = xToCanvas(x);
                      ctx.moveTo(cx, padding);
                      ctx.lineTo(cx, height - padding);
                    }
                    for (let x = xStep; x <= xMax; x += xStep) {
                      const cx = xToCanvas(x);
                      ctx.moveTo(cx, padding);
                      ctx.lineTo(cx, height - padding);
                    }
                    for (let y = 0; y >= yMin; y -= yStep) {
                      const cy = yToCanvas(y);
                      ctx.moveTo(padding, cy);
                      ctx.lineTo(width - padding, cy);
                    }
                    for (let y = yStep; y <= yMax; y += yStep) {
                      const cy = yToCanvas(y);
                      ctx.moveTo(padding, cy);
                      ctx.lineTo(width - padding, cy);
                    }
                    ctx.stroke();
                  }

                  drawGrid();

                  // Axes
                  ctx.lineWidth = 1.4;
                  ctx.strokeStyle = "#6b7280"; // gray-500
                  ctx.beginPath();
                  if (yMin < 0 && yMax > 0) {
                    const y0 = yToCanvas(0);
                    ctx.moveTo(padding, y0);
                    ctx.lineTo(width - padding, y0);
                  }
                  if (xMin < 0 && xMax > 0) {
                    const x0 = xToCanvas(0);
                    ctx.moveTo(x0, padding);
                    ctx.lineTo(x0, height - padding);
                  }
                  ctx.stroke();

                  // Axes ticks and labels
                  ctx.fillStyle = "#6b7280";
                  ctx.font =
                    '10px system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif';

                  function drawTicks() {
                    const xStep = Math.pow(
                      10,
                      Math.floor(Math.log10(xSpan / 8))
                    );
                    const yStep = Math.pow(
                      10,
                      Math.floor(Math.log10(ySpan / 6))
                    );

                    if (yMin < 0 && yMax > 0) {
                      const y0 = yToCanvas(0);
                      for (let x = 0; x >= xMin; x -= xStep) {
                        const cx = xToCanvas(x);
                        ctx.beginPath();
                        ctx.moveTo(cx, y0 - 3);
                        ctx.lineTo(cx, y0 + 3);
                        ctx.stroke();
                        if (x !== 0) ctx.fillText(x.toString(), cx + 2, y0 - 4);
                      }
                      for (let x = xStep; x <= xMax; x += xStep) {
                        const cx = xToCanvas(x);
                        ctx.beginPath();
                        ctx.moveTo(cx, y0 - 3);
                        ctx.lineTo(cx, y0 + 3);
                        ctx.stroke();
                        ctx.fillText(x.toString(), cx + 2, y0 - 4);
                      }
                    }

                    if (xMin < 0 && xMax > 0) {
                      const x0 = xToCanvas(0);
                      for (let y = 0; y >= yMin; y -= yStep) {
                        const cy = yToCanvas(y);
                        ctx.beginPath();
                        ctx.moveTo(x0 - 3, cy);
                        ctx.lineTo(x0 + 3, cy);
                        ctx.stroke();
                        if (y !== 0) ctx.fillText(y.toString(), x0 + 4, cy - 2);
                      }
                      for (let y = yStep; y <= yMax; y += yStep) {
                        const cy = yToCanvas(y);
                        ctx.beginPath();
                        ctx.moveTo(x0 - 3, cy);
                        ctx.lineTo(x0 + 3, cy);
                        ctx.stroke();
                        ctx.fillText(y.toString(), x0 + 4, cy - 2);
                      }
                    }
                  }

                  drawTicks();

                  // Functions
                  const sampleCount = Math.max(400, Math.floor(width)); // more points for wide screens
                  const maxJump = ySpan * 0.25; // threshold to detect discontinuities

                  fnInputs.forEach((entry, idx) => {
                    const inputEl = document.getElementById(entry.id);
                    const visibleEl = document.getElementById(entry.visibleId);
                    const expr = (inputEl.value || "").trim();
                    if (!expr || !visibleEl.checked) return;

                    let fn;
                    try {
                      fn = buildFunction(expr);
                    } catch (e) {
                      showError(e.message);
                      return;
                    }
                    if (!fn) return;

                    ctx.beginPath();
                    ctx.lineWidth = 1.5;
                    ctx.strokeStyle = entry.color;

                    let prevCanvasX = null;
                    let prevCanvasY = null;
                    let prevYVal = null;
                    for (let i = 0; i <= sampleCount; i++) {
                      const t = i / sampleCount;
                      const x = windowState.xMin + t * xSpan;
                      let y;
                      try {
                        y = fn(x);
                      } catch {
                        y = NaN;
                      }
                      if (!isFinite(y)) {
                        prevCanvasX = null;
                        prevCanvasY = null;
                        prevYVal = null;
                        continue;
                      }

                      const cy = yToCanvas(y);
                      const cx = xToCanvas(x);

                      if (
                        prevCanvasX === null ||
                        prevCanvasY === null ||
                        prevYVal === null ||
                        Math.abs(y - prevYVal) > maxJump
                      ) {
                        ctx.moveTo(cx, cy);
                      } else {
                        ctx.lineTo(cx, cy);
                      }

                      prevCanvasX = cx;
                      prevCanvasY = cy;
                      prevYVal = y;
                    }
                    ctx.stroke();
                  });
                }

                // Pan with mouse (desktop)
                let isPanning = false;
                let panStart = null;
                let startWindow = null;

                canvas.addEventListener("mousedown", function (evt) {
                  const rect = canvas.getBoundingClientRect();
                  panStart = {
                    x: evt.clientX - rect.left,
                    y: evt.clientY - rect.top,
                  };
                  startWindow = { ...windowState };
                  isPanning = true;
                });

                window.addEventListener("mousemove", function (evt) {
                  if (!isPanning) return;
                  const rect = canvas.getBoundingClientRect();
                  const curX = evt.clientX - rect.left;
                  const curY = evt.clientY - rect.top;
                  const dx = curX - panStart.x;
                  const dy = curY - panStart.y;
                  const { xMin, xMax, yMin, yMax } = startWindow;
                  const xSpan = xMax - xMin;
                  const ySpan = yMax - yMin;

                  const padding = 32;
                  const width = rect.width;
                  const height = rect.height;

                  const usableWidth = width - 2 * padding;
                  const usableHeight = height - 2 * padding;

                  if (usableWidth <= 0 || usableHeight <= 0) return;

                  const dxVal = (-dx / usableWidth) * xSpan;
                  const dyVal = (dy / usableHeight) * ySpan;

                  windowState.xMin = xMin + dxVal;
                  windowState.xMax = xMax + dxVal;
                  windowState.yMin = yMin + dyVal;
                  windowState.yMax = yMax + dyVal;

                  syncInputsFromState();
                  clearError();
                  draw();
                });

                window.addEventListener("mouseup", function () {
                  isPanning = false;
                });

                window.addEventListener("mouseleave", function () {
                  isPanning = false;
                });

                function doPlot() {
                  clearError();
                  try {
                    parseWindowFromInputs();
                    draw();
                  } catch (e) {
                    showError(
                      e.message || "Unable to plot with the current settings."
                    );
                  }
                }

                btnPlot.addEventListener("click", doPlot);

                btnZoomIn.addEventListener("click", function () {
                  try {
                    parseWindowFromInputs();
                  } catch (e) {
                    showError(e.message);
                    return;
                  }
                  const { xMin, xMax, yMin, yMax } = windowState;
                  const xCenter = (xMin + xMax) / 2;
                  const yCenter = (yMin + yMax) / 2;
                  const newXSpan = (xMax - xMin) / 2;
                  const newYSpan = (yMax - yMin) / 2;
                  windowState.xMin = xCenter - newXSpan / 2;
                  windowState.xMax = xCenter + newXSpan / 2;
                  windowState.yMin = yCenter - newYSpan / 2;
                  windowState.yMax = yCenter + newYSpan / 2;
                  syncInputsFromState();
                  clearError();
                  draw();
                });

                btnZoomOut.addEventListener("click", function () {
                  try {
                    parseWindowFromInputs();
                  } catch (e) {
                    showError(e.message);
                    return;
                  }
                  const { xMin, xMax, yMin, yMax } = windowState;
                  const xCenter = (xMin + xMax) / 2;
                  const yCenter = (yMin + yMax) / 2;
                  const newXSpan = (xMax - xMin) * 2;
                  const newYSpan = (yMax - yMin) * 2;
                  windowState.xMin = xCenter - newXSpan / 2;
                  windowState.xMax = xCenter + newXSpan / 2;
                  windowState.yMin = yCenter - newYSpan / 2;
                  windowState.yMax = yCenter + newYSpan / 2;
                  syncInputsFromState();
                  clearError();
                  draw();
                });

                btnReset.addEventListener("click", function () {
                  windowState = { xMin: -10, xMax: 10, yMin: -10, yMax: 10 };
                  syncInputsFromState();
                  clearError();
                  draw();
                });

                // Presets
                presetParabola.addEventListener("click", function () {
                  document.getElementById("fn1").value = "x^2";
                  document.getElementById("fn2").value = "";
                  document.getElementById("fn3").value = "";
                  windowState = { xMin: -10, xMax: 10, yMin: -10, yMax: 10 };
                  syncInputsFromState();
                  clearError();
                  draw();
                });

                presetSine.addEventListener("click", function () {
                  document.getElementById("fn1").value = "sin(x)";
                  document.getElementById("fn2").value = "";
                  document.getElementById("fn3").value = "";
                  windowState = { xMin: -10, xMax: 10, yMin: -2, yMax: 2 };
                  syncInputsFromState();
                  clearError();
                  draw();
                });

                presetMix.addEventListener("click", function () {
                  document.getElementById("fn1").value = "x^3";
                  document.getElementById("fn2").value = "sin(x)";
                  document.getElementById("fn3").value = "";
                  windowState = { xMin: -5, xMax: 5, yMin: -10, yMax: 10 };
                  syncInputsFromState();
                  clearError();
                  draw();
                });

                // Plot on Enter key from function inputs
                fnInputs.forEach((entry) => {
                  const el = document.getElementById(entry.id);
                  el.addEventListener("keydown", function (evt) {
                    if (evt.key === "Enter") {
                      evt.preventDefault();
                      doPlot();
                    }
                  });
                });

                window.addEventListener("resize", function () {
                  draw();
                });

                // Initial defaults
                document.getElementById("fn1").value = "x^2";
                document.getElementById("fn2").value = "sin(x)";
                document.getElementById("fn3").value = "";
                syncInputsFromState();
                draw();
              })();
            </script>
          </div>



            <section class="mt-10" id="author">
              <h2 class="text-xl font-semibold mb-4">About the author</h2>
              <div class="bg-gray-50 border border-gray-200 rounded-lg p-4">
                <p class="text-sm text-gray-700">
                  <a href="https://www.linkedin.com/in/ugocandido92821/" class="text-blue-600 hover:underline" target="_blank" rel="noopener">Ugo Candido</a>
                  builds financial tools and educational resources to help
                  readers make better money decisions. He focuses on practical,
                  transparent models that reflect how lenders calculate
                  payments and total cost of ownership.
                </p>
                <p class="text-sm text-gray-700 mt-3">
                  Contact: <a href="mailto:info@calcdomain.com" class="text-blue-600 hover:underline">info@calcdomain.com</a>
                </p>
              </div>
            </section>

            <section class="mt-10" id="editorial-policy">
              <h2 class="text-xl font-semibold mb-4">Editorial policy</h2>
              <div class="bg-gray-50 border border-gray-200 rounded-lg p-4">
                <p class="text-sm text-gray-700">
                  CalcDomain content is created for educational purposes and is
                  reviewed for clarity, accuracy, and transparency. We do not
                  accept paid placements that influence calculator outputs.
                  Inputs and assumptions are shown directly in the interface so
                  you can verify how results are produced.
                </p>
              </div>
            </section>

            <section class="mt-10" id="methodology">
              <h2 class="text-xl font-semibold mb-4">Methodology</h2>
              <div class="bg-gray-50 border border-gray-200 rounded-lg p-4">
                <p class="text-sm text-gray-700">
                  Results use standard formulas and the values you provide.
                  Figures are estimates and may differ from lender quotes.
                  For decisions that require professional guidance, consult a
                  licensed advisor.
                </p>
              </div>
            </section>

        
            <section class="mt-10" id="sources">
              <h2 class="text-xl font-semibold mb-4">Sources</h2>
              <div class="bg-gray-50 border border-gray-200 rounded-lg p-4">
                <ul class="list-disc list-inside text-sm text-gray-700 space-y-1">
                  <li>
                    <a href="https://www.nist.gov/pml/owm/metric-si/si-units" class="text-blue-600 hover:underline" target="_blank" rel="noopener">
                      NIST: The International System of Units (SI)
                    </a>
                  </li>
                  <li>
                    <a href="https://openstax.org/details/books/college-algebra" class="text-blue-600 hover:underline" target="_blank" rel="noopener">
                      OpenStax: College Algebra
                    </a>
                  </li>
                </ul>
              </div>
            </section>

        





<section class="bg-white p-4 rounded-lg shadow-md mt-6"><h2 class="text-lg font-semibold mb-2">Inputs used by this calculator</h2><ul class="text-sm text-gray-700 list-disc pl-5"><li>f₁(x) =</li><li>Show f₁(x)</li><li>f₂(x) =</li><li>Show f₂(x)</li><li>f₃(x) =</li><li>Show f₃(x)</li></ul></section><section class="bg-white p-4 rounded-lg shadow-md mt-6"><h2 class="text-lg font-semibold mb-2">Consistency checks</h2><p class="text-sm text-gray-700">Checks: non-negative values, plausible ranges, coherent outputs.</p></section><section class="bg-white p-4 rounded-lg shadow-md mt-6"><h2 class="text-lg font-semibold mb-2">Operational notes</h2><p class="text-sm text-gray-700">Fill in realistic values and keep units and timeframes consistent.</p><p class="text-sm text-gray-700 mt-2">Key entities: Graphing, f₁(x) =, Show f₁(x), f₂(x) =, Show f₂(x), f₃(x) =, Show f₃(x).</p></section><section class="bg-white p-4 rounded-lg shadow-md mt-6"><h2 class="text-lg font-semibold mb-2">Inputs used by this calculator</h2><ul class="text-sm text-gray-700 list-disc pl-5"><li>f₁(x) =</li><li>Show f₁(x)</li><li>f₂(x) =</li><li>Show f₂(x)</li><li>f₃(x) =</li><li>Show f₃(x)</li></ul></section><section class="bg-white p-4 rounded-lg shadow-md mt-6"><h2 class="text-lg font-semibold mb-2">Consistency checks</h2><p class="text-sm text-gray-700">Checks: non-negative values, plausible ranges, coherent outputs.</p></section><!-- %%EEAT_STRIP%% -->
<!-- %%QUALITY_BLOCKS%% -->
<!-- %%FAQ_JSONLD%% -->
</main>

        <aside class="w-full lg:w-1/3">
          <div class="space-y-6">
            <section
              aria-labelledby="related-core-math"
              class="bg-white p-5 rounded-lg shadow-md"
            >
              <h2 id="related-core-math" class="text-lg font-semibold mb-3">
                Related Core Math &amp; Algebra tools
              </h2>
              <ul class="space-y-2 text-sm">
                <li>
                  <a href="/limit" class="text-blue-600 hover:underline"
                    >Limit Calculator</a
                  >
                </li>
                <li>
                  <a href="/derivative" class="text-blue-600 hover:underline"
                    >Derivative Calculator</a
                  >
                </li>
                <li>
                  <a href="/integral" class="text-blue-600 hover:underline"
                    >Integral Calculator</a
                  >
                </li>
                <li>
                  <a href="/taylor-series" class="text-blue-600 hover:underline"
                    >Taylor Series</a
                  >
                </li>
                <li>
                  <a
                    href="/maclaurin-series"
                    class="text-blue-600 hover:underline"
                    >Maclaurin Series Expansion</a
                  >
                </li>
                <li>
                  <a
                    href="/prime-factorization"
                    class="text-blue-600 hover:underline"
                    >Prime Factorization</a
                  >
                </li>
                <li>
                  <a href="/gcf" class="text-blue-600 hover:underline"
                    >GCD &amp; LCM Calculator</a
                  >
                </li>
                <li>
                  <a
                    href="/scientific-notation"
                    class="text-blue-600 hover:underline"
                    >Scientific Notation Converter</a
                  >
                </li>
                <li>
                  <a
                    href="/standard-deviation"
                    class="text-blue-600 hover:underline"
                    >Standard Deviation</a
                  >
                </li>
                <li>
                  <a
                    href="/diophantine-equation"
                    class="text-blue-600 hover:underline"
                    >Diophantine Equation Solver</a
                  >
                </li>
              </ul>
            </section>

            <section class="bg-white p-5 rounded-lg shadow-md">
              <h2 class="text-lg font-semibold mb-3">
                Tips for getting reliable graphs
              </h2>
              <ul class="text-xs text-gray-700 space-y-1.5">
                <li>
                  Start with a symmetric window like −10 to 10 on both axes,
                  then zoom/pan as needed.
                </li>
                <li>
                  Scale the y-range so that important features use at least one
                  third of the vertical height.
                </li>
                <li>
                  For very oscillatory functions, narrow the x-range to isolate
                  just a few periods.
                </li>
                <li>
                  Use separate colors and toggle visibility when comparing
                  multiple functions.
                </li>
                <li>
                  Combine this tool with algebraic work: verify intercepts,
                  asymptotes, and limits analytically where precision matters.
                </li>
              </ul>
            </section>
          </div>
        </aside>
      </div>
    </div>

    <footer class="bg-gray-900 text-white py-12">
      <div class="container mx-auto px-4">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
          <div>
            <h3 class="text-2xl font-bold mb-4">CalcDomain</h3>
            <p class="text-gray-400 mb-4">
              Your trusted source for free online calculators. Accurate, fast,
              and reliable calculations for every need.
            </p>
          </div>
          <div>
            <h4 class="text-lg font-semibold mb-4">Categories</h4>
            <ul class="space-y-2">
              <li>
                <a
                  href="https://calcdomain.com/finance"
                  class="text-gray-400 hover:text-white"
                  >Finance</a
                >
              </li>
              <li>
                <a
                  href="https://calcdomain.com/health-fitness"
                  class="text-gray-400 hover:text-white"
                  >Health & Fitness</a
                >
              </li>
              <li>
                <a
                  href="https://calcdomain.com/math-conversions"
                  class="text-gray-400 hover:text-white"
                  >Math & Conversions</a
                >
              </li>
              <li>
                <a
                  href="https://calcdomain.com/lifestyle-everyday"
                  class="text-gray-400 hover:text-white"
                  >Lifestyle & Everyday</a
                >
              </li>
              <li>
                <a
                  href="https://calcdomain.com/construction-diy"
                  class="text-gray-400 hover:text-white"
                  >Construction & DIY</a
                >
              </li>
            </ul>
          </div>
          <div>
            <h4 class="text-lg font-semibold mb-4">Popular Tools</h4>
            <ul class="space-y-2">
              <li>
                <a
                  href="https://calcdomain.com/mortgage-payment"
                  class="text-gray-400 hover:text-white"
                  >Mortgage Calculator</a
                >
              </li>
              <li>
                <a
                  href="https://calcdomain.com/percentage-calculator"
                  class="text-gray-400 hover:text-white"
                  >Percentage Calculator</a
                >
              </li>
              <li>
                <a
                  href="https://calcdomain.com/bmi-calculator"
                  class="text-gray-400 hover:text-white"
                  >BMI Calculator</a
                >
              </li>
              <li>
                <a
                  href="https://calcdomain.com/auto-loan-calculator"
                  class="text-gray-400 hover:text-white"
                  >Auto Loan Calculator</a
                >
              </li>
              <li>
                <a
                  href="https://calcdomain.com/house-affordability"
                  class="text-gray-400 hover:text-white"
                  >House Affordability</a
                >
              </li>
            </ul>
          </div>
          <div>
            <h4 class="text-lg font-semibold mb-4">Support</h4>
            <ul class="space-y-2">
              <li>
                <a
                  href="https://calcdomain.com/about"
                  class="text-gray-400 hover:text-white"
                  >About Us</a
                >
              </li>
              <li>
                <a
                  href="https://calcdomain.com/contact"
                  class="text-gray-400 hover:text-white"
                  >Contact</a
                >
              </li>
              <li>
                <a
                  href="https://calcdomain.com/privacy"
                  class="text-gray-400 hover:text-white"
                  >Privacy Policy</a
                >
              </li>
              <li>
                <a
                  href="https://calcdomain.com/terms"
                  class="text-gray-400 hover:text-white"
                  >Terms of Service</a
                >
              </li>
              <li>
                <a
                  href="https://calcdomain.com/sitemap.xml"
                  class="text-gray-400 hover:text-white"
                  >Site Map</a
                >
              </li>
            </ul>
          </div>
        </div>
        <div
          class="border-t border-gray-800 mt-8 pt-8 text-center text-gray-400"
        >
          <p>
            &copy; 2025 CalcDomain. All Rights Reserved. | Free Online
            Calculators for Everyone
          </p>
        </div>
      </div>
    </footer>

    <script src="/assets/js/mobile-menu.js"></script>
    <script src="/search.js"></script>

    <script>
      window.MathJax = {
        tex: {
          inlineMath: [
            ["\\(", "\\)"],
            ["$", "$"],
          ],
          displayMath: [
            ["$", "$"],
            ["\\[", "\\]"],
          ],
        },
        svg: { fontCache: "global" },
      };
    </script>
    <script>
  (function () {
    var loadMathJax = function () {
      if (window.__mathjaxLoading) return;
      window.__mathjaxLoading = true;
      var script = document.createElement("script");
      script.id = "MathJax-script";
      script.async = true;
      script.src = "https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-chtml.js";
      document.head.appendChild(script);
    };

    var target = document.querySelector(
      ".formula-box, .mathjax, .math, .formula, [data-mathjax]"
    );

    if (!target) return;

    if ("IntersectionObserver" in window) {
      var observer = new IntersectionObserver(
        function (entries) {
          if (entries.some(function (entry) { return entry.isIntersecting; })) {
            observer.disconnect();
            loadMathJax();
          }
        },
        { rootMargin: "200px" }
      );
      observer.observe(target);
    } else {
      window.addEventListener(
        "load",
        function () {
          setTimeout(loadMathJax, 1000);
        },
        { once: true }
      );
    }
  })();
</script>
  
<script async src="https://www.googletagmanager.com/gtag/js?id=G-7MB5V1LZRN"></script>
<script>
  window.__cmpLoadQueued = window.__cmpLoadQueued || [];
  window.addEventListener("load", function () {
    var loadCmp = function () {
      var s1 = document.createElement("script");
      s1.src = "https://cmp.gatekeeperconsent.com/min.js";
      s1.async = true;
      s1.setAttribute("data-cfasync", "false");
      document.head.appendChild(s1);

      var s2 = document.createElement("script");
      s2.src = "https://the.gatekeeperconsent.com/cmp.min.js";
      s2.async = true;
      s2.setAttribute("data-cfasync", "false");
      document.head.appendChild(s2);
    };

    if ("requestIdleCallback" in window) {
      requestIdleCallback(loadCmp, { timeout: 2000 });
    } else {
      setTimeout(loadCmp, 1000);
    }
  });
</script>
<script async src="//www.ezojs.com/ezoic/sa.min.js"></script>
<script>
      window.ezstandalone = window.ezstandalone || {};
      ezstandalone.cmd = ezstandalone.cmd || [];
    </script>
</body>
</html>
