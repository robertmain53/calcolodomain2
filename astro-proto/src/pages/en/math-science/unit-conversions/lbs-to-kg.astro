---
import fs from "node:fs";
import path from "node:path";
import Layout from "../../../../components/Layout.astro";
import SeoHead from "../../../../components/SeoHead.astro";
import CalculatorIsland from "../../../../components/CalculatorIsland.astro";

const filePath = path.join(process.cwd(), "src", "legacy", "calculators", "lbs-to-kg.html");
let raw = fs.readFileSync(filePath, "utf8");
raw = raw
  .replace(/<script[^>]*tailwindcss[^>]*><\/script>/gi, "")
  .replace(/<header[\s\S]*?<\/header>/i, "");
const stripScripts = /<script(?![^>]*application\/ld\+json)[\s\S]*?<\/script>/gi;
raw = raw.replace(stripScripts, "");
const titleMatch = raw.match(/<title>([^<]*)<\/title>/i);
const descMatch = raw.match(/<meta\s+name="description"\s+content="([^"]*)"/i);
const bodyMatch = raw.match(/<body[^>]*>([\s\S]*?)<\/body>/i);
const title = titleMatch?.[1]?.trim() || "Pounds to Kilograms Converter";
const description =
  descMatch?.[1]?.trim() ||
  "Convert pounds to kilograms with historical context and mass/weight distinction.";
const body = bodyMatch?.[1] || "";
---
<Layout title={title} description={description}>
  <Fragment slot="head">
    <SeoHead
      title={title}
      description={description}
      slug={["math-science", "unit-conversions", "lbs-to-kg"]}
    />
    <link rel="stylesheet" href="/vendor/tailwind.min.css" />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </Fragment>

  <div class="legacy" set:html={body}></div>

  <section class="enhanced">
    <h2>Enhanced lb ↔ kg (with gravity)</h2>
    <p class="lede">Reactive converter with gravity selector and guidance on mass vs weight to reinforce the legacy tool.</p>
    <CalculatorIsland slug="lbs-to-kg" />
  </section>
</Layout>

<style>
  .enhanced {
    margin-top: 1.5rem;
    padding: 1rem;
    border: 1px solid #1f2937;
    border-radius: 12px;
    background: rgba(255, 255, 255, 0.03);
  }
  .enhanced .lede {
    color: #94a3b8;
  }
  .legacy header svg,
  .legacy nav svg {
    max-width: 24px;
    max-height: 24px;
  }
</style>