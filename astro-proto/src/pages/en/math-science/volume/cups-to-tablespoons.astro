---
import fs from "node:fs";
import path from "node:path";
import Layout from "../../../../components/Layout.astro";
import SeoHead from "../../../../components/SeoHead.astro";
import CalculatorIsland from "../../../../components/CalculatorIsland.astro";

const filePath = path.join(process.cwd(), "..", "sito_modificato", "cups-to-tablespoons.html");
const raw = fs.readFileSync(filePath, "utf8");
const titleMatch = raw.match(/<title>([^<]*)<\/title>/i);
const descMatch = raw.match(/<meta\s+name="description"\s+content="([^"]*)"/i);
const bodyMatch = raw.match(/<body[^>]*>([\s\S]*?)<\/body>/i);
const title = titleMatch?.[1]?.trim() || "Cups to Tablespoons Converter";
const description =
  descMatch?.[1]?.trim() ||
  "Convert cups and tablespoons with regional standards and milliliter output.";
const body = bodyMatch?.[1] || "";
---
<Layout title={title} description={description}>
  <Fragment slot="head">
    <SeoHead
      title={title}
      description={description}
      slug={["math-science", "volume", "cups-to-tablespoons"]}
    />
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </Fragment>

  <div class="legacy" set:html={body}></div>

  <section class="enhanced">
    <h2>Enhanced cups ↔ tablespoons</h2>
    <p class="lede">Reactive converter with selectable standards and mL output to accompany the legacy content.</p>
    <CalculatorIsland slug="cups-to-tablespoons" />
  </section>
</Layout>

<style>
  .enhanced {
    margin-top: 1.5rem;
    padding: 1rem;
    border: 1px solid #1f2937;
    border-radius: 12px;
    background: rgba(255, 255, 255, 0.03);
  }
  .enhanced .lede {
    color: #94a3b8;
  }
</style>
